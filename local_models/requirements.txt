# ============================================================
# IP Agent 本地模型依赖
#
# 安装方式:
#   conda activate ip_agent_gpu
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#   pip install -r local_models/requirements.txt
#
# 适用环境: RTX 5060 8GB (Blackwell) / Python 3.10+ / CUDA 12.8
# 最后更新: 2026-07-05
# ============================================================

# ---- 深度学习框架 (先单独安装) ----
# torch>=2.8.0         # 单独安装: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# torchvision>=0.23.0
# torchaudio>=2.8.0

# ---- 核心工具 ----
numpy>=1.24.0
requests>=2.31.0
psutil>=5.9.0
pywin32>=305               # Windows: 进程管理

# ---- LLM INT4 量化 (8GB 显存适配) ----
transformers>=5.0.0
bitsandbytes>=0.43.0       # INT4 量化，~0.5GB 显存
accelerate>=0.20.0
sentencepiece>=0.2.0
tiktoken>=0.5.0            # Qwen tokenizer 依赖

# ---- 语音识别 ASR (FunASR SenseVoiceSmall) ----
funasr>=1.0.0
modelscope>=1.14.0
addict                     # modelscope 隐式依赖
datasets                   # modelscope 隐式依赖
simplejson                 # modelscope jsonplus
sortedcontainers           # modelscope pipeline

# ---- TTS 语音合成 (CosyVoice) ----
# CosyVoice 仓库需单独 git clone 到项目根目录
soundfile>=0.12.0
HyperPyYAML
inflect
wetext
conformer
onnxruntime                # ONNX 推理 (CosyVoice 用 CPU，无需 GPU 版)
x-transformers
pyworld
omegaconf
diffusers
onnx
librosa>=0.10.0
openai-whisper              # MuseTalk 依赖 (音频特征提取)
matplotlib
gdown
pytorch_lightning
networkx
hydra-core
scikit-learn

# ---- 视频处理 ----
opencv-python>=4.8.0

# ---- 视频下载 ----
yt-dlp>=2024.0.0

# ---- LipSync 口型同步 ----
# MuseTalk: 需 clone MuseTalk_repo/ 到项目根目录
# Wav2Lip: 需 clone Wav2Lip_repo/ 到项目根目录
face_alignment>=1.5.0      # 人脸关键点检测 (替代 mmpose)
pydub                       # 音频格式转换
scikit-image                # 图像处理

# ---- Web UI ----
gradio>=4.0.0

# ---- 抖音自动发布 ----
playwright>=1.40.0          # 浏览器自动化
# 安装后需执行: playwright install chromium

# ---- API 服务 ----
fastapi>=0.109.0
uvicorn>=0.20.0             # ASGI 服务器

# ---- 开发/调试 ----
pillow>=10.0.0

